# Behaviour shared by every machine.

set -g history-limit 10000
set -g mouse on

# Copy to the system clipboard through the terminal's OSC 52 support, which
# also works over SSH where there is no local X display.
set -s set-clipboard on
