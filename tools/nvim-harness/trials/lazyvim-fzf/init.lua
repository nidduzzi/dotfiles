-- Trial config B: LazyVim with the fzf-lua picker extra replacing snacks.picker.
-- Run it without touching the daily config:
--   NVIM_APPNAME=lazyvim-fzf XDG_CONFIG_HOME=<this dir's parent> nvim
require 'config.lazy'
