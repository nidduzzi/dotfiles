-- Trial config A: stock LazyVim, whose picker is snacks.picker.
-- Run it without touching the daily config:
--   NVIM_APPNAME=lazyvim-snacks XDG_CONFIG_HOME=<this dir's parent> nvim
require 'config.lazy'
